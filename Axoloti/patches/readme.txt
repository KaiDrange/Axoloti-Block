BlocksProgramChanger.axs - Controller subpatch (add it to Axoloti preferences) that cycles through the patches with two of the pushbuttons.

BlocksControls.axs - subpatch for the hardware knobs and buttons (except the two pushbuttons used for cycling through patches)

startup.axp - a tiny patch that gets loaded on powerup and then switches to the first patch in the patch bank.
